# Feature Status

Use `Persistent`, `Partial`, `Local`, or `Planned`. Update this file in the same pull request that changes a status.

The **Data path** column records which service reaches the database, because moving every path to `FastAPI` is the outstanding work from the 2026-08-20 decision in `docs/DECISIONS.md`.

Every admin list below renders through the shared `DataTable` in `frontend/src/components/common/data-table.tsx`, which supplies search, per-column sorting, multi-select filters, column visibility, pagination, and the loading and empty states. Where a row below names filters or a search, those are that screen's configuration of the shared table rather than a private implementation.

| Module | Status | Data path | Current boundary | Next backend step |
|---|---|---|---|---|
| Authentication/RBAC | Persistent | FastAPI / Prisma fallback | Password-only sign-in (`/register`, `/account/password`, admin set-password, `npm run db:set-password`), 4 roles (`STUDENT`, `PLACEMENT_VOLUNTEER`, `PLACEMENT_TEAM`, `SUPER_ADMIN`), a 49-permission `module.action` catalog declared once in `frontend/src/lib/permissions.ts` and mirrored in `backend/app/core/security.py`, per-user custom permission grants and revocations, user provisioning, suspension, emergency `ADMIN_EMAILS` bootstrap. Only `SUPER_ADMIN` holds `rbac.manage`, so only it can assign roles or edit the permission matrix. Students hold `_own` permissions and are held out of the admin portal by `hasAnyAdminPermission`, which tests permission membership rather than list length. Every admin route is registered in `ROUTE_PERMISSIONS` (longest-prefix match), which both the middleware and the admin sidebar read | Verify production callback/cookies and deployment secrets; add mailbox verification and a forgot-password flow before external exposure |
| Database/schema | Persistent | Prisma (owner) | Migrations and seed exist; seed creates accounts from `ADMIN_EMAILS` | Add migrations with each schema change |
| Dashboard | Persistent | FastAPI (cached) / Prisma for own rows | Drives and announcements come from `GET /api/v1/jobs?activeOnly=true` and `GET /api/v1/announcements?limit=20`, both read through Redis. The endpoint returns every ACTIVE drive and the page drops the passed deadlines, so the clock stays out of the cache key. Drafts are excluded by the server, not by a filter the page passes. The caller's own applications and resume count are still Prisma, being per-user and not worth caching | Port the per-user counts to FastAPI |
| Company Events | Persistent | FastAPI (cached) / Prisma direct for detail | The list reads `GET /api/v1/jobs`, cached in Redis: ACTIVE and ENDED, soonest deadline first, with the company name in the row. `/company-events/[id]` is still Prisma. Per-student eligibility is never cached — `GET /api/v1/jobs/{id}` caches the drive and recomputes eligibility per request | Port the detail page; add attachments and selected-resume association |
| Apply flow | Persistent | FastAPI / Prisma fallback | Server-side eligibility check over CGPA, batch, branch, degree, gender, backlogs, bans, and documents; resume selection; unique Application creation | Add custom application questions if required |
| Applications | Persistent | FastAPI / Prisma fallback | User-owned applications, stage timeline, resume association, and guarded withdrawal persist | Add interview slot booking |
| Profile | Persistent | FastAPI / Prisma fallback | Identity and core profile fields with AES-256-GCM encrypted Aadhaar/PAN numbers and encrypted identity document files with challenge-unlock preview. Name, roll number, branch, degree, and graduation year are roster-owned and read-only to the student on both the UI and the `PATCH /profile` schema. Gender, blood group, and active backlogs are closed-list selects (values in `frontend/src/lib/profile-schema.ts`, mirrored in `backend/app/schemas/student.py`); percentages are 0–100 and CGPA 0–10, each capped at two decimal places and enforced independently on both sides | Add profile picture upload if needed |
| Resumes | Persistent | FastAPI / Local & Cloud storage | Typed list, upload with custom labels, rename, delete, secure in-portal PDF preview, and application attachment | Add multi-version comparison |
| Feedback | Persistent | FastAPI / Prisma fallback | Student submission, support history with type & status filters, response viewer; Admin response & resolve workflow with notifications and email dispatch | Add conversation threads if required |
| Forms/NOC | Persistent | FastAPI / Prisma fallback | Typed list, NOC creation with validation, request cancellation, request details modal showing both the student's own remarks and the placement cell's decision remarks, and official placement policy downloads | Add multi-department sign-off chain if needed |
| Contact/Team | Persistent | FastAPI / Prisma fallback | Public team and contact presentation with dynamic database records | Add photo uploads via Cloudinary if needed |
| Admin Team | Persistent | FastAPI / Prisma fallback | Authorized team management, display reordering, linked user RBAC auto-grant/revoke, and configurable default placement team permissions | Add coordinator cohort history if needed |
| Admin dashboard | Persistent | FastAPI (no fallback) | One season-scoped read of `GET /api/v1/analytics/admin/overview`: placements, PPOs and internships, highest/average/median/lowest package, per-degree and per-branch distributions, top recruiters, application funnel, recent applications. The season selector defaults to the newest season that has an offer | Add CSV/PDF export of the season report; add year-on-year comparison |
| Admin placement records | Persistent | FastAPI | `/admin/placement-records`: authorized create, edit, and guarded delete of `Offer` rows (student, company, optional drive, type, status, job title, CTC/stipend, location, dates, remarks), with season/company/degree/type/status filters, search, and an Excel download of the season. `/admin/placement-records/add` records one drive's outcome for a pasted list of roll numbers through `POST /api/v1/offers/bulk`, which returns a partial result: every roll number it could not record comes back with a reason. Writes require `placement_records.create`/`.update`, and deletion `placement_records.delete` | Accept a file upload on the bulk screen, rather than only a paste |
| Admin companies | Persistent | Prisma direct | Three routes under one sidebar group: `/admin/companies` lists with category and session filters, and `/admin/companies/add` and `/admin/companies/[id]/edit` share one titled form card (company name, a closed two-value category of Dream and First Round, a placement-session year dropdown, free-text turnover, required company info, and a duplicate-check acknowledgement that gates creation). Writes require `companies.create`/`companies.update` and deletion `companies.delete`, which is refused while any event references the company | Port to FastAPI; restore website and logo editing (see the 2026-09-18 decision), add recruiter contacts |
| Admin events | Persistent | Prisma direct | Three routes under one sidebar group: `/admin/events` lists with status, placement-year, batch, and type filters; `/admin/events/add` and `/admin/events/[id]/edit` share one full-page form (placement year and batch year dropdowns, company search dialog, closed four-value category list without "Core", employment type, a `react-day-picker` calendar with quarter-hour slots for the deadline, degree and grouped-branch checkbox dialogs offered from the roster, an optional-eligibility toggle covering genders and placement bans, cap/bond/duration/redirect-link terms, a per-event "Additional Questions" builder — `EventQuestionBuilder`, `frontend/src/components/admin/event-question-builder.tsx` — for Text/MCQ/Checkbox/File Upload questions stored on `JobProfile.questions`, and an `EventAttachmentUploader` dropzone (`.pdf`/`.png`, 4 MB, 5 files) stored on `JobProfile.attachments`, both JSON). Writes require `jobs.create`/`jobs.update`, publishing to `ACTIVE` also requires `jobs.publish`, and deletion `jobs.delete`. `/admin/job-profiles` is a permanent redirect. Writes go to Prisma, so each one also calls `POST /api/v1/cache/invalidate` to drop the API's cached event lists | Port to FastAPI; add a point of contact; collect and store a student's answers to the additional questions (needs an `Application` column); show a drive's attachments on the student-facing detail page (nothing reads `JobProfile.attachments` yet) |
| Admin students | Persistent | Prisma direct / FastAPI (flags) | Authorized searchable directory and read-only detail; directory also shows a "needs follow-up" badge and filter, backed by `GET /api/v1/students/admin/flags`, for students eligible for 3+ companies in a row (chronologically) with no application to any of them | Port the rest of the directory to FastAPI; add cohort filters and export |
| Admin applications | Persistent | FastAPI / Prisma fallback | Authorized candidate list, filtering by job/status/branch, single & bulk stage progression, and CSV export served by `GET /api/v1/applications/admin/export` through the `/api/admin/applications/export` route handler, honouring the on-screen filters and search | Add custom stage emails; add an Excel format if the office needs one |
| Admin announcements | Persistent | FastAPI / Prisma fallback | Three routes under one sidebar group: `/admin/announcements/company-event` and `/admin/announcements/general` compose through a season → company → event → tag funnel that keeps the editor locked until the fields above it are filled, and `/admin/announcements` manages active against draft with publish/withdraw, edit, preview, filters, search, and guarded delete. The body is a shadcn-styled Tiptap rich text editor (`components/ui/rich-text-editor.tsx`), not a plain textarea; `Announcement.content` stores sanitized HTML (allow-list enforced by both the Zod schema and the Pydantic schema, and again at every render site), and every list preview, search index, and character-limit check strips tags back to plain text first. A company event stores the drive it is about in `Announcement.jobProfileId`; drafts are invisible to anyone without an administrative footing (`announcements.view` alone is not enough — every student holds it, so the gate tests `hasAnyAdminPermission` first). Reads are cached in Redis, keyed by whether the caller may see drafts, and every write clears the topic. Up to 10 attachments per announcement (pdf, png, jpg, csv, txt, doc, docx, xls, xlsx; 10 MB each) upload through `POST /api/v1/uploads/admin/announcement-attachment`, are validated against the file's own bytes, and are readable by students only once the announcement is published | Add a rich-text editor; dispatch email on publish |
| Admin NOC requests | Persistent | FastAPI / Prisma fallback | Authorized searchable & filtered list, student academic inspect, approve/reject with decision remarks stored in `adminRemarks` separately from the student's `message`, signed PDF upload and in-portal preview, student in-app and email notifications | Add multi-authority digital signature workflow |
| Admin Feedbacks | Persistent | FastAPI / Prisma fallback | Authorized query & feedback management, type & resolution filters, response modal with resolve toggle, student notification and email dispatch, delete action | Add automated FAQ suggestions |
| Interview Experiences | Persistent | FastAPI | Student submission (company, role, batch, interview type, per-category question fields) held `PENDING` until reviewed; student browse of `APPROVED` submissions and own-submission status tracking; admin moderation (`/admin/interview-experiences`) with approve/reject guarded by `interview_experiences.approve` and delete by `interview_experiences.delete`, student notification and email dispatch on review | Add per-submission edit for admins, and richer full-text search/filtering |
| File uploads | Persistent | FastAPI (Cloudinary / Local fallback) | PDF/size validation, local disk fallback with UUID naming, and authenticated streaming route | Add S3 provider option if required |
| Email/notifications | Persistent | FastAPI / Resend fallback | In-app notification creation with non-blocking email dispatch and graceful dummy-key handling | Add email template customization |
| Encryption | Persistent utility | n/a | AES-256-GCM helper and tests exist in both services; active on Aadhaar and PAN | Integrate into other sensitive fields as needed |
| CI/Docker | Persistent | n/a | Per-service Dockerfiles, prod and dev Compose stacks, one-shot migration container, health routes, three-job CI. Verified end to end: all images build and the stack reaches healthy with migrations and admin seeding applied. | Add a deployment target and production secrets |
| Database table browser | Persistent | `sqladmin` over the existing SQLAlchemy models, mounted at `/admin` on the backend by `backend/app/admin/`. All 15 tables, grouped into five sidebar categories, every column readable on the details page and editable on the form — `passwordHash` and the identity ciphertexts included, since it holds no key and shows ciphertext either way. Views are generated from the mapper, so a mirrored migration needs no edit here. Search is an `ILIKE` over text columns only; enums are excluded because Postgres will not match one. A created row generates its own key, because Prisma's `cuid()` runs client-side and `id` has no database default; a meaningful key like `SystemSetting.key` is asked for instead. Guarded only by `DB_ADMIN_PASSWORD` — empty is off, under 16 characters is refused, and a refusal logs without touching the API. Dark mode follows the OS by default and cycles light/dark/system from a top-bar toggle, sharing the portal's `tnp-theme` storage key; the theme is set in an inline head script so there is no flash, and select2 and flatpickr are restyled from Tabler's variables because their vendored stylesheets are light-only. Verified: login rejects a wrong password, all 15 list pages return 200, the User details page carries all 41 columns, create, edit, and delete round-trip against Postgres, and both themes were measured in a browser down to the calendar and tag-field colours. | Record who changed which row; `sqladmin` emits audit events this does not yet consume |

## UI component migration

Persistence is unaffected by this; the table above still describes the data paths.

| Surface | Component layer | Boundary | Next step |
| --- | --- | --- | --- |
| Admin composer forms | shadcn/ui | `company-form.tsx` and `event-form.tsx` are Card/Label/Input/Textarea/Checkbox/Switch/Select/ToggleGroup/Popover/Command; `react-day-picker` still supplies the deadline month grid directly, since shadcn's `Calendar` targets v9 and this repo runs v10 | Adopt shadcn `Calendar` if it moves to react-day-picker 10 |
| Dialogs, both sides | shadcn/ui | All render through `PortalDialog` → Radix (renamed from `AdminDialog` once the student side joined), which added the focus trap and Escape handling none of them had. `announcement-composer.tsx` keeps its inline `composer-field` dropdowns and the `useDismissOnOutsideClick` hook | Convert the composer's inline dropdowns to `Popover` and retire the hook |
| Admin export column picker | shadcn/ui | `export-columns-dialog.tsx` groups the columns, locks the required ones behind a lock icon, offers a per-group checkbox and two columns of optional fields, scrolls its body inside an 85vh cap, and keeps `Apply (N columns)` on a fixed footer. Only the registrations table configures it | Offer it on the students and offers exports |
| Admin status banners | shadcn/ui | Every `.admin-success`/`.admin-error`/`.admin-info` div is now `Alert`, with repo-added `success` and `info` variants so the status hues survive | — |
| Admin data tables | shadcn, all 12 tables | All twelve tables across eleven screens still render through one `DataTable` (`frontend/src/components/common/data-table.tsx`), which owns the section heading, the row count, the actions row, and the card, and still runs the same hand-written search/sort/filter/paginate pipeline (`frontend/src/lib/data-table.ts`). Its render layer is now shadcn throughout — `Table`, `Popover`+`Command` for filters (search box on every filter, not just long lists), `DropdownMenu` for column visibility, `Select` for rows-per-page, `Button` for every table-owned control — reading this portal's tokens through the existing `@theme inline` bridge, per the 2026-09-20 decision. Column pinning, persisted column visibility, and the bulk-select leading column (Applications) are unchanged | Prune the now-dead interactive-chrome CSS (`.dt-filter-button`, `.dt-popover*`, `.dt-check`, `.dt-sort`, `.dt-rows-*`, `.dt-page-buttons`) from `admin.css` |
| Student portal | shadcn/ui | Converted: `profile-view`, `forms-view`, `dashboard-feed`, `interview-experiences-view`, `feedback-form`, `feedback-list`, `jobs-list`, `apply-button`, `applications-view`, and the login/register/set-password pages. Every `.modal-backdrop` is gone; controls are `Input`/`Label`/`Select`/`Textarea`/`Button`/`Card`/`Badge`/`Alert`/`Tabs`/`Accordion`/`ToggleGroup`. Each converted `Select` posts through an explicit hidden input. The page shells (`.module-page`, `.login-page`) stay hand-written; the nav rail no longer does | Retire the leftover shell classes that now only wrap a Card |
| Navigation shells, both sides | shadcn/ui | `admin-shell.tsx` and `portal-shell.tsx` render `ui/sidebar`: drawer below 768px, collapse to an icon rail, `Cmd/Ctrl+B`, cookie-persisted state, and tooltips on the rail. Admin groups are `Collapsible` + `SidebarMenuSub` and still prune against `allowedPaths`, so a group with no permitted children disappears. Rows are 15px on 40px and sub-rows 14px on 36px, up from 12px and 11.5px; the rail is 17.5rem. `--sidebar-*` alias the semantic tokens rather than adding a palette, and the active row keeps the institute blue with its 3px marker. 173 lines of hand-written sidebar CSS were deleted | Watch the highlight follow a real navigation, and see the rail beside a wide data grid |
| Student interview experiences | Persistent, rebuilt UI | Submission is five collapsible `Accordion` groups over the same 17 fields, with a live "N of 17 sections filled" count, per-group badges, and submit disabled until one is filled. Browse and My submissions are `Tabs`; cards carry topic tags derived from which sections were filled; search covers answer text, not just company and role; the reader groups filled sections under their headings | Let a reader filter by more than one topic at once |

## Open blockers

*None currently blocking core student or admin workflows.*

