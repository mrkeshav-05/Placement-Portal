fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
# Database table browser mounted at /admin. Drives the existing SQLAlchemy
# models, so it adds a view over the current data layer rather than a new one.
sqladmin>=0.20
# Signs the /admin session cookie (Starlette's SessionMiddleware needs it).
itsdangerous>=2.2
asyncpg>=0.30
# Read-through cache for the announcement and event endpoints. The asyncio
# client ships in the same package, so this adds no second dependency.
redis>=5.0
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.0.0
python-jose[cryptography]>=3.3
cryptography>=43
python-multipart>=0.0.9
cloudinary>=1.40
resend>=2.0
httpx>=0.27
# Sanitizes announcement rich text server-side, mirroring the allow-list the
# frontend's DOMPurify pass already applies — this is the belt for a request
# built by hand rather than through the composer's editor.
bleach>=6.1
pytest>=8.0
pytest-asyncio>=0.23
