import{createCipheriv,createDecipheriv,randomBytes}from"node:crypto";const ALGORITHM="aes-256-gcm";function getKey(keyHex=process.env.ENCRYPTION_KEY){if(!keyHex||!/^[a-fA-F0-9]{64}$/.test(keyHex))throw new Error("ENCRYPTION_KEY must be a 64-character hexadecimal value");return Buffer.from(keyHex,"hex")}export function encryptSensitiveValue(value:string,keyHex?:string){const iv=randomBytes(12);const cipher=createCipheriv(ALGORITHM,getKey(keyHex),iv);const encrypted=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return[iv.toString("hex"),cipher.getAuthTag().toString("hex"),encrypted.toString("hex")].join(":")}export function decryptSensitiveValue(payload:string,keyHex?:string){const[ivHex,tagHex,dataHex]=payload.split(":");if(!ivHex||!tagHex||!dataHex)throw new Error("Invalid encrypted payload");const decipher=createDecipheriv(ALGORITHM,getKey(keyHex),Buffer.from(ivHex,"hex"));decipher.setAuthTag(Buffer.from(tagHex,"hex"));return Buffer.concat([decipher.update(Buffer.from(dataHex,"hex")),decipher.final()]).toString("utf8")}
