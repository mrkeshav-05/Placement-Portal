# Claude Project Instructions

Read and follow `AGENTS.md` first, then `docs/PROJECT_CONTEXT.md` and `docs/FEATURE_STATUS.md`. Those files are canonical. Do not infer current architecture from `IMPLEMENTATION_PLAN.md` when it conflicts with the code or canonical context.

When a change alters architecture or feature maturity, update `docs/DECISIONS.md` or `docs/FEATURE_STATUS.md` in the same change.
